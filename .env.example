# Generated automatically for local development by `npx convex dev --once`.
NEXT_PUBLIC_CONVEX_URL=
NEXT_PUBLIC_CONVEX_SITE_URL=
CONVEX_DEPLOYMENT=

# Optional local-only key used by the documented demo seed command.
# Set the same value in Convex with `npx convex env set SCANME_DEMO_SETUP_KEY <value>`.
SCANME_DEMO_SETUP_KEY=
