import type { VenueDict } from "../types";

// Public venue page copy. Empty-but-typed: no venue UI exists yet (TASK-06+), so
// no copy is invented. Keys are added here when the screens are built.
export const venueSr = {} as const satisfies VenueDict;
