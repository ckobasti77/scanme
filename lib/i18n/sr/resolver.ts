import type { ResolverDict } from "../types";

// Card-resolver copy (/r/nevazeca). Empty-but-typed: the resolver route does not
// exist yet (TASK-10). Keys are added when the route is built.
export const resolverSr = {} as const satisfies ResolverDict;
